Feature requests, bugs, and other tasks.

- [x] When we convert a video, the collapsed state of our tree view should be remembered when the view reloads.
- [x] When a video is converted/edited and gets a new child in the tree, it should default to being expanded rather than collapsed.
