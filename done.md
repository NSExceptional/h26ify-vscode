- [x] There are some build errors; please fix them first.
- [x] Currently, we track which videos have been "adjusted" by our extension by putting `.hevc.` in the filename. That is, videos with .hevc in the name are assumed to be a product of adjusting some other video. Instead, we should use workspace storage or local storage or whatever it's called and use that as a way to track videos, based on some reasonable metadata about each file. Furthermore…
- [x] When video A is adjusted and produces video B, we should make note of that relationship in the metadata we store from the previous task, and make video B appear as a 'child' of video A in our tree view. And so on and so forth.
    - [x] Be sure to handle cases where videos are deleted or renamed in the folder; we don't want to adjust the hierarchy metadata. Renaming a file should keep the original name in the hierarchy metadata and indicate that the file could not be found. Changing the name back should have the tree instantly update to show the file as "found" again.
- [x] We need to implement a way to trim, crop, and resize videos. I'm not sure what the flows or interactions for any of these should be; give me some ideas.
    - [x] There should also be a way to apply a set of operations against multiple videos at once (i.e. applying the same resize and crop to a bunch of videos all at once)

- [x] Currently, we "key" videos in our storage by their path. But this means someone can delete `video.mp4` and later add a new, completely different `video.mp4` and our metadata will pick it up as the same video. We should take more data about the video into account so that these kinds of mixups can't occur. Perhaps a hash of the filename, mtime, and filesize? Something like that? Open to better ideas.
    - [x] Take the filesystem's case-sensitivity into account. If we're on a case-sensitive filesystem, use the lowercase, workspace-relative path of the video, so that changing `video.MOV` to `video.mov` on a case-insensitive fs does not disrupt our perceived "identity" of the video, since the fs itself would treat them as the same / conflicting names.

- [x] The current layout is a little confusing. We have videos grouped by HEVC / not, but then when we convert a video to HEVC (and the original is not deleted), it appears under the parent video under the non-HEVC section. What we need to do is add a third group for "All Videos" and do the nesting there, and there only — no nesting under the filetype-specific ones. Please also put the "All Videos" section above the other two.
